// --- START OF FILE config.js ---

export const PAGE_SPACING = 18;         // Vertical distance between page centers
export const TRANSITION_DURATION = 800; // Animation time in ms (MUST match ScrollManager debounce)
export const CAMERA_Z_OFFSET = 12;      // How far the camera is from the page plane
export const BACKGROUND_COLOR = 0x050a14;// Background color
export const FOG_NEAR = 25;
export const FOG_FAR = 70;
export const PAGE_SCALE_INACTIVE = 0.85;// Scale of non-focused pages
export const PAGE_Z_INACTIVE = -2;      // Z-position of non-focused pages

// --- END OF FILE config.js ---